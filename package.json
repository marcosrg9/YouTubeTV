{
  "name": "youtube-tv",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build-x64": "tauri build --target x86_64-pc-windows-msvc",
    "build-aarch64": "tauri build --target aarch64-pc-windows-msvc",
    "build native": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
